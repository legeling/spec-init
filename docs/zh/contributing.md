# 贡献指南

维护一个核心 Skill，双语模板和示例保持一致。改规则时优先替换造成问题的旧规则，避免不断叠加通用约束。

脚本或模板变化运行 `npm run lint` 和 `npm test`。核心行为变化还应试用相关场景，检查实际实施、文档改动范围和历史方案处理；措辞匹配不能替代行为验收。

PR 说明问题、最终行为、兼容影响和实际验证即可。普通修改无需先创建 Issue、编号链或独立变更文档。
