# 发票筛选

状态：示例中的有效约定；不宣称已有对应实现。

用户可以只看逾期且未收款的发票。到期日按项目已确认的业务时区计算，筛选保持原排序，不触发通知。

验收条件：已收款与未来到期的发票不出现；筛选不改变原数据；空结果显示零条。真实项目应在这里引用实际测试路径和命令。

尚未执行任何测试：此目录只有文档示例。实施时先明确时区与到期边界，再实现并验证上述条件，无需复制到多层需求、设计和覆盖矩阵。
