# Design: __PROJECT_NAME__

> 更新时间：__DATE__
>
> 这份文档解释 how，不再重复 why。

## 1. 设计目标

- [为了满足哪些需求，需要具备哪些能力]
- [本设计优先优化什么，例如简单性、性能、稳定性]

## 2. 系统边界

- 输入：[用户输入 / 外部事件 / 第三方系统]
- 输出：[页面 / API / 文件 / 通知]
- 外部系统：[依赖项]
- 不在本系统内的内容：[明确边界]

## 3. 架构概览

[可以写成文字说明，或补充 Mermaid / 架构图]

## 3.1 候选方案与用户确认

当技术栈、系统形态或关键架构方向存在多种合理选择时，不要直接自己定。至少列出：

| 议题 | 方案 | 优点 | 代价 / 风险 | 推荐意见 | 是否已获用户确认 |
|---|---|---|---|---|---|
| [例如前后端一体 / 前后端分离] | [方案 A] | [优点] | [代价] | [为什么推荐] | [是 / 否] |

如果这里仍然是“否”，不要把该决策当作既定事实写进实现。

## 4. 模块设计

| ID | 模块 | 职责 | 对应需求 |
|---|---|---|---|
| `DES-001` | [模块名] | [职责] | `FR-001`, `FR-002` |
| `DES-002` | [模块名] | [职责] | `FR-003` |

## 5. 数据与状态

- 核心实体：[实体名与职责]
- 关键字段：[只列关键字段和含义]
- 状态转换：[状态变化规则]

## 6. 接口与契约

| 接口 | 输入 | 输出 | 错误场景 | 对应需求 |
|---|---|---|---|---|
| [接口 1] | [输入] | [输出] | [失败情况] | `FR-001` |

## 7. 技术决策与权衡

- 决策：[选择了什么]
- 备选方案：[对比过什么]
- 为什么不选：[取舍原因]

## 7.1 技术栈与基础设施

- 前端 / 客户端：[技术栈或无]
- 后端 / 服务端：[技术栈或无]
- 数据存储：[数据库 / 缓存 / 文件 / 队列]
- 测试框架：[单元 / 集成 / E2E]
- 部署形态：[单体 / 微服务 / Serverless / 本地 CLI / 桌面应用]

说明：

- 这里必须写清楚“为什么选这个栈”，而不是只列名字。
- 如果技术栈还没定，也要把候选方案和利弊写清楚，并标明是否已向用户确认。

## 7.2 架构质量目标

- 可维护性：[如何保证后续好改]
- 性能目标：[延迟 / 吞吐 / 资源占用]
- 安全目标：[鉴权、权限、输入校验、敏感信息保护]
- 可测试性：[白盒、集成、回归、性能、安全测试如何落地]

重要决策建议同步记录到 `docs/adr/`。

## 8. 需求映射说明

逐条说明设计如何覆盖关键需求：

- `DES-001` 如何满足 `FR-001`
- `DES-002` 如何满足 `FR-002`
- 如果某条需求暂时没有设计承接，显式标出来

## 9. 风险与缓解

- 风险：[可能出问题的点]
- 缓解：[如何提前防范]

## 10. 追踪矩阵

| Requirement | Design |
|---|---|
| `FR-001` | `DES-001` |
| `FR-002` | `DES-001` |

## 11. 设计自检

- 每条高优需求都能在这里找到承接它的设计项
- 这里在讲“怎么做”，而不是重复“为什么做”
- 这里没有把任务清单、排期和碎执行步骤混进来
- 关键风险和取舍已经被显式记录
- 技术栈、架构方向和质量目标已经写清楚，而不是停留在“以后再说”
- 对存在分歧的关键方案，已经给出利弊并标明是否获得用户确认

## 12. 待确认

- [待确认]
- [待确认]

## 不该写进这里的内容

- 纯产品背景复述
- 太细的执行任务清单
- “先做什么后做什么”的排期细节
