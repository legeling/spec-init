# 文档入口

目前尚未登记权威功能文档或活动计划；这是骨架，不代表已完成规范或实现。

每个主题登记一份权威文档，保存最新确认的需求与验收，沿用已有路径；新主题可按需使用 `specs/<topic>.md`。确认改需求就原位替换旧说法，未完成的实现标注“待实施”。其他现行文档引用此处指定的真源，不再重复定义。
跨会话工作按需登记活动计划（如 `plans/<topic>.md`），只记动作和进度并引用权威文档，完成或失效后移出活动入口。
历史仅用于追溯，明确失效及现行入口，不提供当前指令。旧项目不自动迁移或删除文件。
