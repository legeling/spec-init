interface:
  display_name: "spec-init"
  short_description: "Kick off a project with requirements, design, implementation plan, TDD, tasks, README, and AGENTS scaffolding."
  brand_color: "#0F766E"
  default_prompt: "Use $spec-init to kick off a new project with spec-first documentation and TDD-friendly scaffolding before implementation starts."

policy:
  allow_implicit_invocation: true
